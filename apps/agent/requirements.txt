# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.0.0
python-multipart>=0.0.9
websockets>=12.0

# Local STT (optional — chỉ cần khi STT_PROVIDER=local)
faster-whisper>=1.0.0

# LiveKit Python SDK (optional — chỉ cần khi tích hợp real-time audio)
livekit>=0.12.0
